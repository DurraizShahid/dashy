{
  "name": "@hivemind/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo ok",
    "docker:build": "docker build -t hivemind-worker ."
  },
  "dependencies": {
    "@hivemind/core": "workspace:*",
    "@hivemind/connectors": "workspace:*",
    "@hivemind/shared": "workspace:*",
    "bullmq": "^5.40.0",
    "pino": "^9.6.0",
    "ioredis": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
